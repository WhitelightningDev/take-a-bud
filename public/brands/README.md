# Brand logos

Add the official brand logo files here so the landing page can display them.

Expected filenames:
- `public/brands/lifted.png`
- `public/brands/awaken-distillates.png`

Tips:
- Use transparent backgrounds (PNG/SVG) if possible.
- Keep them wide, not tall (height ~ 34px on the page).

